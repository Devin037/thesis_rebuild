/**
 * config.js
 * * Stores configuration constants for the sorting game.
 */

// Path to the CSV file containing the quiz questions.
// This path is relative to the root index.html file.
export const CSV_FILE_PATH = 'sortingGame/quiz_questions.csv';

// Delay in milliseconds after a card is revealed before the user can interact with it again.
export const REVEAL_DELAY_MS = 100;



